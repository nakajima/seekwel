[workspace]
members = ["seekwel-derive"]

[package]
name = "seekwel"
version = "0.1.3"
edition = "2024"
description = "an experimental sqlite library"
license = "BSD-3-Clause"

[dependencies]
rusqlite = { version = "0.39.0", features = ["bundled-sqlcipher"] }
seekwel-derive = { path = "seekwel-derive" }
